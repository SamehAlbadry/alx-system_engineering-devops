#!/bin/bash

# Use ls to list all directories (including hidden ones) in the current directory
# Exclude the current and parent directories, and count the number of results
dir_count=$(ls -d .*/ */ 2>/dev/null | grep -v -E '/(\.|..)/' | wc -l)

